# Kubernetes / Helm
kubeconfig
*.kubeconfig
kubeconfig*.yaml
# TimeWeb kubeconfig (содержит креды/токен кластера — НИКОГДА не коммитить, репо публичный)
twc-*-config.yaml
*-config.yaml
charts/
*.tgz

# Secrets / local overrides
*.local.yaml
secrets.yaml
.env

# k6 / результаты прогона (summary коммитим вручную в docs/)
*.bin
results/

# OS / редакторы
.DS_Store
.idea/
.vscode/
*.swp
